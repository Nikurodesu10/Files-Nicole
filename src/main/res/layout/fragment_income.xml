<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#E6FFEB"
    android:padding="16dp">

    <!-- Back Button -->
    <ImageButton
        android:id="@+id/button_back"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:padding="8dp"
        android:src="@drawable/ic_back"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:layout_alignParentStart="true"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp"
        android:contentDescription="Back"/>

    <!-- Edit Button -->
    <ImageButton
        android:id="@+id/button_edit"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:padding="8dp"
        android:src="@drawable/ic_edit"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="8dp"
        android:layout_marginTop="8dp"
        android:contentDescription="Edit"/>

    <!-- Title -->
    <TextView
        android:id="@+id/text_income_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Income"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:textColor="#000000"/>

    <!-- Sale of Sugar Section -->
    <TextView
        android:id="@+id/text_sale_sugar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="1. Sale of Sugar - 190,000\n   95 sacks"
        android:textSize="16sp"
        android:layout_below="@id/text_income_title"
        android:layout_marginTop="32dp"
        android:textColor="#000000"/>

    <!-- Molasses Section -->
    <TextView
        android:id="@+id/text_molasses"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="2. Molasses - 98,000\n   55 kilos"
        android:textSize="16sp"
        android:layout_below="@id/text_sale_sugar"
        android:layout_marginTop="24dp"
        android:textColor="#000000"/>

    <!-- Tracking Incentives Section -->
    <TextView
        android:id="@+id/text_tracking_incentives"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="3. Tracking Incentives - 75,000\n   15 tons"
        android:textSize="16sp"
        android:layout_below="@id/text_molasses"
        android:layout_marginTop="24dp"
        android:textColor="#000000"/>

    <!-- Total -->
    <TextView
        android:id="@+id/text_total"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Total: 363,000"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:textColor="#4CAF50"/>
</RelativeLayout>
